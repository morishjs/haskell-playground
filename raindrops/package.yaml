name: raindrops
version: 1.1.0.6

dependencies:
  - base

library:
  exposed-modules: Raindrops
  source-dirs: src
  ghc-options: -Wall
  dependencies:
   - containers       # List here the packages you
  # - bar       # want to use in your solution.

tests:
  test:
    main: Tests.hs
    source-dirs: test
    dependencies:
      - raindrops
      - hspec
